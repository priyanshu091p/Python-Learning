# Python Learning - Day 2 🐍

## 📌 Topics Covered

* Arithmetic Operators
* Assignment Operators
* Comparison Operators
* Logical Operators

---

# ➕ Arithmetic Operators

Arithmetic operators are used to perform mathematical operations.

| Operator | Meaning        | Example  |
| -------- | -------------- | -------- |
| `+`      | Addition       | `a + b`  |
| `-`      | Subtraction    | `a - b`  |
| `*`      | Multiplication | `a * b`  |
| `/`      | Division       | `a / b`  |
| `%`      | Remainder      | `a % b`  |
| `//`     | Floor Division | `a // b` |
| `**`     | Power          | `a ** b` |

Example:

```python
a = 10
b = 3

print(a + b)
print(a % b)
print(a ** 2)
```

---

# 📝 Assignment Operators

Assignment operators are used to assign and update values.

| Operator | Example  | Meaning             |
| -------- | -------- | ------------------- |
| `=`      | `x = 10` | Assign value        |
| `+=`     | `x += 5` | Add and assign      |
| `-=`     | `x -= 5` | Subtract and assign |
| `*=`     | `x *= 5` | Multiply and assign |
| `/=`     | `x /= 5` | Divide and assign   |

Example:

```python
x = 10

x += 5

print(x)
```

Output:

```
15
```

---

# ⚖️ Comparison Operators

Comparison operators compare two values and return `True` or `False`.

| Operator | Meaning               |
| -------- | --------------------- |
| `==`     | Equal to              |
| `!=`     | Not equal to          |
| `>`      | Greater than          |
| `<`      | Less than             |
| `>=`     | Greater than or equal |
| `<=`     | Less than or equal    |

Example:

```python
a = 10
b = 5

print(a > b)
```

Output:

```
True
```

---

# 🧠 Logical Operators

Logical operators are used to combine conditions.

| Operator | Meaning                        |
| -------- | ------------------------------ |
| `and`    | Both conditions must be True   |
| `or`     | Any one condition must be True |
| `not`    | Reverse the result             |

Example:

```python
age = 20

print(age > 18 and age < 30)
```

Output:

```
True
```

---

# 💻 Practice Questions

## 🟢 Easy (1-5)

1. Add two numbers.
2. Subtract two numbers.
3. Multiply two numbers.
4. Divide two numbers.
5. Find the remainder.

---

## 🟡 Medium (6-10)

6. Find the square of a number.

7. Find the cube of a number.

8. Check whether one number is greater than another.

9. Find the average of three numbers.

10. Calculate simple interest.

Formula:

```
SI = (P × R × T) / 100
```

---

## 🔥 Challenge (11-15)

11. Convert seconds into hours, minutes, and seconds.

12. Calculate electricity bill using a simple formula.

13. Calculate salary after a 10% bonus.

14. Convert rupees into dollars using a fixed exchange rate.

15. Create a basic calculator using operators.

*(Without using if statements)*

---

# 🚀 Mini Project

## Student Result Calculator

### Features:

* Input student name
* Input marks of 5 subjects
* Calculate total marks
* Calculate percentage
* Print all details neatly

Example Output:

```
Student Result

Name: Priyanshu
Marks:
Python: 85
C++: 80
Java: 75
Math: 90
English: 88

Total: 418
Percentage: 83.6%
```

---

# 🛠️ Tools Used

* Python
* VS Code
* Git & GitHub

---

# 👨‍💻 Author

**Priyanshu**

BCA Student
